# Clear Speed

Gigabit-capable browser speed test for Clear Technology Solutions.

Deploy command:

```bash
npx wrangler deploy
```

No database, storage bucket, API key, or manually-created binding is required.
The rate-limit bindings are created from `wrangler.json` during deployment.
